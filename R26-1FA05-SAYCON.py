import math # To be able to calculate the distance and the formula sqrt and pow
x1 = float(int(input("Enter x1: ")))
y1 = float(int(input("Enter x1: ")))
x2 = float(int(input("Enter x1: ")))
y2 = float(int(input("Enter x1: ")))

# Calculate the distance
d = x1, x2, y1, y2
print(math.sqrt((math.pow((x2 - x2),2) + math.pow((y2 - y1),2))))

# Reflection: Using a library was more practical and made the code easier to make.
# 1. It had many shortcuts.
# 2. sqrt() and pow()
# 3. The code would be too long and complicated.
